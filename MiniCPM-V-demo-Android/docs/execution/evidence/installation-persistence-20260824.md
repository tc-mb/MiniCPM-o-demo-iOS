# 固定签名覆盖安装持久化验收（2026-08-24）

设备：vivo V2359A。安装方式仅使用 `adb install -r`，未卸载应用、未清除应用数据。

验收顺序：

1. 在应用私有 `noBackupFilesDir` 写入安装前聚合基线；
2. 覆盖安装同一构建批次的主 APK 与测试 APK；
3. 安装后重新读取并逐项比较；
4. 删除测试基线文件，确认测试探针不残留。

比较项仅包含会话数、消息数、知识库数、文档数及各状态数量、E5/Guard 模型哈希、HNSW 加密索引文件数量和总字节数。测试不读取或输出用户问题、回答、文件名、正文或 URI。

结果：采集测试 `1/1` 通过，覆盖安装后验证测试 `1/1` 通过；全部聚合属性一致，基线文件成功删除，应用冷启动成功（`TotalTime: 585 ms`）。
